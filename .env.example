# Backend
HTTP_ADDR=:8080
DATABASE_PATH=data/omepic.db
REDIS_URL=redis://localhost:6379/0

# Plaintext UID prefix before encryption; trailing underscores are normalized.
UID_PREFIX=omeo_
UID_ENCRYPTION_KEY=change-me-uid-secret
JWT_SECRET=change-me-too
